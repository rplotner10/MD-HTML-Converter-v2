# User Guide
Steps in order to use the mdConverter
    1. Have a file with markdown text you wish to convert into html format, in the working directory.
    2. Store this file in a variable your name of choosing.
    3. Create an MDConverter object, give it a name.
    4. Call complieDoc through the object you just created, with the variable within the parenthesis.